$('#renderEditUserObserverForm').append(
    '<%= j render(
        partial: 'form',
        locals:{
            telegram_username_observer: @telegram_username_observer
        })%>'
);
clearIsInvalid();

$('#editUserObserver').on('show.bs.modal', function (e) {
  enableSelect2();
});

$('#editUserObserver').modal('show');

