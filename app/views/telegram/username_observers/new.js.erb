$('#renderNewUserObserverForm').append(
    '<%= j render(
        partial: 'form',
        locals:{
            telegram_username_observer: @telegram_username_observer
        })%>'
);
clearIsInvalid();

$('#newUserObserver').on('show.bs.modal', function (e) {
  enableSelect2();
});

$('#newUserObserver').modal('show');

chooseSurvWP();
