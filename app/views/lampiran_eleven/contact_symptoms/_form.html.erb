<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = new_lampiran_eleven_close_contact_information_info_exposes_officer_contact_symptom_path(@lampiran_eleven_close_contact_information,@lampiran_eleven_info_exposes_officer)
  method = "post"
%>
<% else %>
<% url = lampiran_eleven_close_contact_information_info_exposes_officer_contact_symptom_path(@lampiran_eleven_close_contact_information,@lampiran_eleven_info_exposes_officer)
  method = "patch"
%>
<% end %>
<%= form_for(lampiran_eleven_contact_symptom, url: url, :method => method) do |form| %>
  <% if lampiran_eleven_contact_symptom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lampiran_eleven_contact_symptom.errors.count, "error") %> prohibited this lampiran_eleven_contact_symptom from being saved:</h2>

      <ul>
      <% lampiran_eleven_contact_symptom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :is_sore_throat %>
    <%= form.check_box :is_sore_throat %>
  </div>

  <div class="field">
    <%= form.label :date_of_sore_throat %>
    <%= form.date_select :date_of_sore_throat %>
  </div>

  <div class="field">
    <%= form.label :is_cough %>
    <%= form.check_box :is_cough %>
  </div>

  <div class="field">
    <%= form.label :date_of_cough %>
    <%= form.date_select :date_of_cough %>
  </div>

  <div class="field">
    <%= form.label :is_flu %>
    <%= form.check_box :is_flu %>
  </div>

  <div class="field">
    <%= form.label :date_of_flu %>
    <%= form.date_select :date_of_flu %>
  </div>

  <div class="field">
    <%= form.label :is_out_of_breath %>
    <%= form.check_box :is_out_of_breath %>
  </div>

  <div class="field">
    <%= form.label :date_of_out_of_breath %>
    <%= form.date_select :date_of_out_of_breath %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
