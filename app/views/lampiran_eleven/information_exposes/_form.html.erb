
<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = lampiran_eleven_close_contact_information_information_exposes_path(@lampiran_eleven_close_contact_information)
  method = "post"
%>
<% else %>
<% url = lampiran_eleven_close_contact_information_information_expose_path(@lampiran_eleven_close_contact_information)
  method = "patch"
%>
<% end %>
<%= form_for(lampiran_eleven_information_expose, url: url, :method => method) do |form| %>
  <% if lampiran_eleven_information_expose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lampiran_eleven_information_expose.errors.count, "error") %> prohibited this lampiran_eleven_information_expose from being saved:</h2>

      <ul>
      <% lampiran_eleven_information_expose.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :main_type_contact_id %>
    <% if (params[:action]=="new") || (params[:action]=="create") %>
      <%= form.collection_select(:main_type_contact_id, Main::TypeContact.all, :slug, :type_contact, {:prompt => 'Choose Contact Type'}, {class: "form-control", required: true, "data-toggle": "select", "title": "Example (Belum Menikah)", "data-live-search": "true", "data-live-search-placeholder": "Cari ..."}) %>
    <% else %>
      <%= form.collection_select(:main_type_contact_id, Main::TypeContact.all, :slug, :type_contact, {:prompt => 'Choose Contact Type', selected: @lampiran_eleven_information_expose.type_contact.slug}, {class: "form-control", required: true, "data-toggle": "select", "title": "Example (Belum Menikah)", "data-live-search": "true", "data-live-search-placeholder": "Cari ..."}) %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :other_type_contact %>
    <%= form.text_field :other_type_contact %>
  </div>

  <div class="field">
    <%= form.label :date_contact %>
    <%= form.date_select :date_contact %>
  </div>

  <div class="field">
    <%= form.label :duration_contact %>
    <%= form.time_select :duration_contact %>
  </div>

  <div class="field">
    <%= form.label :main_set_location_id %>
    <% if (params[:action]=="new") || (params[:action]=="create") %>
      <%= form.collection_select(:main_set_location_id, Main::SetLocation.all, :slug, :set_location, {:prompt => 'Choose Set Location'}, {class: "form-control", required: true, "data-toggle": "select", "title": "Example (Belum Menikah)", "data-live-search": "true", "data-live-search-placeholder": "Cari ..."}) %>
    <% else %>
      <%= form.collection_select(:main_set_location_id, Main::SetLocation.all, :slug, :set_location, {:prompt => 'Choose Set Location', selected: @lampiran_eleven_information_expose.set_location.slug}, {class: "form-control", required: true, "data-toggle": "select", "title": "Example (Belum Menikah)", "data-live-search": "true", "data-live-search-placeholder": "Cari ..."}) %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :other_set_location %>
    <%= form.text_field :other_set_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
