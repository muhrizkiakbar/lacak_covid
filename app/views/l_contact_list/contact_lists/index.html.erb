<p id="notice"><%= notice %></p>

<h1>L Contact List Contact Lists</h1>

<table>
  <thead>
    <tr>
      <th>Date contact</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @l_contact_list_contact_lists.each do |l_contact_list_contact_list| %>
      <tr>
        <td><%= l_contact_list_contact_list.date_contact %></td>
        <td>
        <%= link_to 'New L Contact List Contact Patient', main_patient_new_contact_patient_path(l_contact_list_contact_list.patient,l_contact_list_contact_list) %>
        </td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

