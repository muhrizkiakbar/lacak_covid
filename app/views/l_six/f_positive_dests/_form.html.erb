
<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = l_six_first_second_third_fourth_f_positive_dests_path(@l_six_first,@l_six_second,@l_six_third,@l_six_fourth)
  method = "post"
%>
<% else %>
<% url = l_six_first_second_third_fourth_f_positive_dest_path(@l_six_first,@l_six_second,@l_six_third,@l_six_fourth)
  method = "patch"
%>
<% end %>
<%= form_for(l_six_f_positive_dest, url: url, :method => method) do |form| %>
  <% if l_six_f_positive_dest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(l_six_f_positive_dest.errors.count, "error") %> prohibited this l_six_f_positive_dest from being saved:</h2>

      <ul>
      <% l_six_f_positive_dest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="field">
    <%= form.label :relation %>
    <%= form.text_field :relation %>
  </div>

  <div class="field">
    <%= form.label :date_contact %>
    <%= form.date_select :date_contact %>
  </div>

  <div class="field">
    <%= form.label :last_date_contact %>
    <%= form.date_select :last_date_contact %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
