<%= content_for :judul_page do %>
  Identitas Pasien
<% end %>
<%= content_for :breadcumb do %>
    <ol class="breadcrumb breadcrumb-links">
        <li class="breadcrumb-item">
          <%= link_to l_six_firsts_path do %>
            <%= icon("fas", "file-alt") %>
          <% end %>
        </li>
        <li class="breadcrumb-item">
          <%= link_to 'Form 6', @l_six_first %>
        </li>
        <li class="breadcrumb-item active" aria-current="page">A</li>
    </ol>
<% end %>
<% content_for :header_button do%>
    <%= link_to 'Back', 
        l_six_first_path, 
        class: 'btn btn-sm btn-neutral'%>
<% end %>
<% content_for :isi do %>
    <div class="container-fluid mt--6">
        <div class="card">
            <div class="card-header">
                <h3 class="mb-0">A. Identitas Pasien Edit</h3>
            </div>
            <div class="card-body">
                <%= render 'form', l_six_first: @l_six_first %>
            </div>
        </div>
    </div>
<% end %>
<% if notice %>
  <div class="notifikasi" data-notifikasi="<%= notice %>"></div>
<% end %>
<%= render partial: 'layouts/body' %>