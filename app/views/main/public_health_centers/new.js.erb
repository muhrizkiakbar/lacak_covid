$('#renderNewPHCForm').append(
    '<%= j render(
        partial: 'form',
        locals:{
            main_public_health_center: @main_public_health_center
        })%>'
);
$('#newPHC').on('show.bs.modal', function (e) {
  enableSelect2();
});

$('#newPHC').modal('show');