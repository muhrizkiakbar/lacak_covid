<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = main_dinkes_province_dinkes_region_hospital_public_health_centers_path(@main_dinkes_province,@main_dinkes_region,@main_hospital)
  method = "post"
%>
<% else %>
<% url = main_dinkes_province_dinkes_region_hospital_public_health_center_path(@main_dinkes_province,@main_dinkes_region,@main_hospital)
  method = "patch"
%>
<% end %>
<%= form_for(main_public_health_center, url: url, :method => method) do |form| %>
  <% if main_public_health_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_public_health_center.errors.count, "error") %> prohibited this main_public_health_center from being saved:</h2>

      <ul>
      <% main_public_health_center.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :public_health_center %>
    <%= form.text_field :public_health_center %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
