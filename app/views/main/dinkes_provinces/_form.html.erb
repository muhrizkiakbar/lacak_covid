<%= form_with(model: main_dinkes_province, local: true) do |form| %>
  <% if main_dinkes_province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_dinkes_province.errors.count, "error") %> prohibited this main_dinkes_province from being saved:</h2>

      <ul>
      <% main_dinkes_province.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dinkes_province %>
    <%= form.text_field :dinkes_province %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
