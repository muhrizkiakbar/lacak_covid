$('#renderNewCityForm').append(
    '<%= j render(
        partial: 'form',
        locals:{
            main_province: @main_province,
            main_city: @main_city
        })%>'
);
clearIsInvalid();
$('#newCity').modal('show');
