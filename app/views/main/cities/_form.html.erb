<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = main_province_cities_path(main_province)
  method = "post"
%>
<% else %>
<% url = main_province_city_path(main_province)
  method = "patch"
%>
<% end %>
<%= form_for(main_city, url: url, :method => method, remote: true) do |form| %>
  <% if main_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_city.errors.count, "error") %> prohibited this main_city from being saved:</h2>

      <ul>
      <% main_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-body">
    <div class="form-group">
      <%= form.text_field :city, autocomplete: 'off', class: 'form-control', placeholder: 'Nama Kabupaten/Kota'  %>
      <div class="invalid-feedback"></div>
    </div>
  </div>

  <div class="modal-footer">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
  </div>
<% end %>
