<%= form_with(model: main_marital_status, remote: true) do |form| %>
  <% if main_marital_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_marital_status.errors.count, "error") %> prohibited this main_marital_status from being saved:</h2>

      <ul>
      <% main_marital_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-body">
    <div class="form-group">
      <%= form.text_field :marital_status, autocomplete: 'off', class: 'form-control', placeholder: 'Status Perkawinan' %>
      <div class="invalid-feedback"></div>
    </div>
  </div>

  <div class="modal-footer">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
  </div>
<% end %>
