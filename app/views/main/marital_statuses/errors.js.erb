<% if @main_marital_status.errors.any? %>
    <% @main_marital_status.errors.details.each_with_index do |message, key| %>
        objId = $("#main_<%= message[0] %>_<%= message[0] %>");
        objId.closest('.form-group').addClass('has-danger');
        objId.siblings('.invalid-feedback').text("<%= @main_marital_status.errors.full_messages[key].html_safe %>");
        objId.addClass('is-invalid');
    <% end %>
<% end %>