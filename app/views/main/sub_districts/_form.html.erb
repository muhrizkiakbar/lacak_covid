<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = main_province_city_district_sub_districts_path(@main_province,@main_city,@main_district)
  method = "post"
%>
<% else %>
<% url = main_province_city_district_sub_district_path(@main_province,@main_city,@main_district)
  method = "patch"
%>
<% end %>
<%= form_for(main_sub_district, url: url, :method => method, remote: true) do |form| %>
  <% if main_sub_district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_sub_district.errors.count, "error") %> prohibited this main_sub_district from being saved:</h2>

      <ul>
      <% main_sub_district.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-body">
    <div class="form-group">
      <%= form.text_field :sub_district, autocomplete: 'off', class: 'form-control', placeholder: 'Nama Kelurahan' %>
      <div class="invalid-feedback"></div>
    </div>
  </div>

  <div class="modal-footer">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
  </div>
<% end %>
