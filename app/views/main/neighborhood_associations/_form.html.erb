<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = main_province_city_district_sub_district_citizen_association_neighborhood_associations_path(@main_province,@main_city,@main_district,@main_sub_district,@main_citizen_association)
  method = "post"
%>
<% else %>
<% url = main_province_city_district_sub_district_citizen_association_neighborhood_association_path(@main_province,@main_city,@main_district,@main_sub_district,@main_citizen_association)
  method = "patch"
%>
<% end %>
<%= form_for(main_neighborhood_association, url: url, :method => method) do |form| %>
  <% if main_neighborhood_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_neighborhood_association.errors.count, "error") %> prohibited this main_neighborhood_association from being saved:</h2>

      <ul>
      <% main_neighborhood_association.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :neighborhood_association %>
    <%= form.text_field :neighborhood_association %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
