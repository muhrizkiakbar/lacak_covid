<% if (params[:action]==="new") || (params[:action]==="create") %>
<% url = main_dinkes_province_dinkes_region_hospitals_path(@main_dinkes_province,@main_dinkes_region)
  method = "post"
%>
<% else %>
<% url = main_dinkes_province_dinkes_region_hospital_path(@main_dinkes_province,@main_dinkes_region)
  method = "patch"
%>
<% end %>
<%= form_for(main_hospital, url: url, :method => method) do |form| %>
  <% if main_hospital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_hospital.errors.count, "error") %> prohibited this main_hospital from being saved:</h2>

      <ul>
      <% main_hospital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hospital %>
    <%= form.text_field :hospital %>
  </div>

  <div class="field">
    <%= form.label :is_primary %>
    <%= form.check_box :is_primary %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
