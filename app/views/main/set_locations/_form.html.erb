<%= form_with(model: main_set_location, remote: true) do |form| %>
  <% if main_set_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_set_location.errors.count, "error") %> prohibited this main_set_location from being saved:</h2>

      <ul>
      <% main_set_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-body">
    <div class="form-group">
      <%= form.text_field :set_location, autocomplete: 'off', class: 'form-control', placeholder: 'Nama Lokasi' %>
      <div class="invalid-feedback"></div>
    </div>
  </div>

  <div class="modal-footer">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
  </div>
<% end %>
