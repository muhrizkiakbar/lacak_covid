<% if @main_type_contact.errors.any? %>
    <% @main_type_contact.errors.details.each_with_index do |message, key| %>
        objId = $("#main_<%= message[0] %>_<%= message[0] %>");
        objId.closest('.form-group').addClass('has-danger');
        objId.siblings('.invalid-feedback').text("<%= @main_type_contact.errors.full_messages[key].html_safe %>");
        objId.addClass('is-invalid');
    <% end %>
<% end %>