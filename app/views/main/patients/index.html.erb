

<% content_for :header_button do%>
    <% if policy('Main::Patient'.to_sym).new? %>
      <%= link_to 'New', new_main_patient_path,
      class: 'btn btn-sm btn-neutral',
      data: {'toggle': 'modal', }, remote: true %>
    <% end %>
    <button type="button" class="btn btn-sm btn-neutral" data-toggle="modal" data-target="#filterPatient">
      Filter
    </button>
<% end %>

<% content_for :isi do %>
  <div class="container-fluid mt--6">
    <div class="card">
      <div class="card-header border-0">
        <div class="row">
          <div class="col">
            <h3 class="mb-0">Daftar Pasien</h3>
          </div>
        </div>
      </div>
      <div class="table-responsive">
        <table class="table align-items-center table-flush table-striped">
          <thead class="thead-light">
            <tr class="d-flex">
              <th class="col-1"></th>
              <th class="col-1">No. KTP</th>
              <th class="col-1">Nama Lengkap</th>
              <th class="col-1 text-center">Tanggal Lahir</th>
              <th class="col-3">Alamat KTP</th>
              <th class="col-3">Alamat Domisili</th>
              <th class="col-1">Kabupaten/Kota</th>
              <th class="col-1">Kecamatan</th>
              <th class="col-1">Kelurahan</th>
              <th class="col-1 text-center">RW</th>
              <th class="col-1 text-center">RT</th>
              <th class="col-1">Status Pernikahan</th>
              <th class="col-1">Nama Orang Tua</th>
              <th class="col-1 text-center">Nomor Telepon</th>
              <th class="col-1 text-center">Jenis Kelamin</th>
            </tr>
          </thead>
          <tbody>
            <% @main_patients.each do |main_patient| %>
            <tr class="d-flex">
              <td class="col-1" class="text-left">
                <%# if policy('LContactList::ContactList'.to_sym).new? %>
                    <%= link_to main_patient_new_contact_list_path(main_patient), 
                      class: 'table-action', remote: true,
                      data: {'toggle': 'tooltip', 'original-title': 'Add Contact List'} do %>
                      <% icon('far', 'id-card') %>
                    <% end %>
                <%# end %>
                <%# if policy('Main::Patient'.to_sym).show? %>
                    <%= link_to main_patient_path(main_patient),
                      class: 'table-action',
                      data: {'toggle': 'tooltip', 'original-title': 'Show Contact List'} do %>
                      <% icon('far', 'eye') %>
                    <% end %>
                <%# end %>
                <% if policy('Main::Patient'.to_sym).edit? %>
                    <%= link_to edit_main_patient_path(main_patient),
                      class: 'table-action', remote: true,
                      data: {'toggle': 'tooltip', 'original-title': 'Edit'} do %>
                      <% icon('far', 'edit') %>
                    <% end %>
                <% end %>
                <% if policy('Main::Patient'.to_sym).destroy? %>
                    <%= link_to main_patient, method: :delete,
                      data: {
                        confirm: 'Are you sure?',
                        'toggle': 'tooltip',
                        'original-title': 'Delete',
                        'bootstrap-confirm': 'delete' },
                      class: 'table-action' do %>
                      <% icon('fas', 'trash') %>
                    <% end %>
                <% end %>
              </td>
              <td class="col-1"><%= main_patient.no_identity %></td>
              <td class="col-1"><%= main_patient.name %></td>
              <td class="col-1 text-center"><%= main_patient.date_of_birth %></td>
              <td class="col-3 text-wrap">
                <% if current_user.public_health_center.nil? %>
                  Disembunyikan.
                <% else %>
                  <%= main_patient.address %>
                <% end %>
              </td>
              <td class="col-3 text-wrap">
                <% if current_user.public_health_center.nil? %>
                  Disembunyikan.
                <% else %>
                  <%= main_patient.residence_address %>
                <% end %>
              </td>
              <td class="col-1"><%= main_patient.city.city %></td>
              <td class="col-1"><%= main_patient.district.district %></td>
              <td class="col-1"><%= main_patient.sub_district.sub_district %></td>
              <td class="col-1 text-center"><%= main_patient.citizen_association.citizen_association %></td>
              <td class="col-1 text-center"><%= main_patient.neighborhood_association.neighborhood_association %></td>
              <td class="col-1"><%= main_patient.marital_status.marital_status %></td>
              <td class="col-1"><%= main_patient.name_of_parent %></td>
              <td class="col-1 text-center"><%= main_patient.phone_number %></td>
              <td class="col-1 text-center"><%= main_patient.gender == 'Male' ? 'Laki-laki' : 'Perempuan' %></td>
              
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="card-footer">
        <%= paginate @main_patients %>
      </div>
    </div>
  </div>

  <div class="modal fade modal-new" id="newPatient" tabindex="-1" role="dialog" aria-labelledby="newPatientLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="newPatientLabel">New Pasien</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div id="renderNewPatientForm"></div>
      </div>
    </div>
  </div>

  <div class="modal fade modal-edit" id="editPatient" tabindex="-1" role="dialog" aria-labelledby="editPatientLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editPatientLabel">Edit Pasien</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div id="renderEditPatientForm"></div>
      </div>
    </div>
  </div>

  <div class="modal fade modal-new" id="newContact" tabindex="-1" role="dialog" aria-labelledby="newContactLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="newContactLabel">New Contact List</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div id="renderNewContactForm"></div>
      </div>
    </div>
  </div>

  <div class="modal fade modal-edit" id="editContact" tabindex="-1" role="dialog" aria-labelledby="editContactLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editContactLabel">Edit Contact List</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div id="renderEditContactForm"></div>
      </div>
    </div>
  </div>

  <%= form_tag( main_patients_path, :method => "get") do %>
      <div class="modal fade modal-new" id="filterPatient" tabindex="-1" role="dialog" aria-labelledby="filterPatientLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-xl" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="filterPatientLabel">Filter Pasien</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
                <div class="row">
                  <div class="col-xl-4">
                    <div class="form-group">
                      <label for="main_patient_no_identity" class="form-control-label">No. KTP</label>
                      <%= text_field_tag :no_identity, nil, class: 'form-control', placeholder: 'No. KTP', value: @no_identity %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_name" class="form-control-label">Nama Lengkap</label>
                      <%= text_field_tag :name, nil, class: 'form-control', placeholder: 'Nama Lengkap', value: @name %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_date_of_birth" class="form-control-label">Tanggal Lahir</label>
                      <%= text_field_tag :date_of_birth, nil, class: 'form-control datepicker', value: @date_of_birth %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_address" class="form-control-label">Alamat KTP</label>
                      <%= text_area_tag :address, nil, class: 'form-control', rows: 3, autocomplete: "off", value: @name %>
                    </div>
                  </div>
                  <div class="col-xl-4">

                    <div class="form-group">
                      <label for="main_patient_main_city_id" class="form-control-label">Kabupaten/Kota</label>
                      <% if (params[:main_city_id].nil?) || (params[:main_city_id] == "") %>
                        <%= select_tag :main_city_id,
                          options_from_collection_for_select(
                          Main::City.all,
                          :slug,
                          :city),
                          :prompt => 'Pilih',
                          class: "form-control kabupaten_kota",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_city_id,
                          options_from_collection_for_select(
                          Main::City.all,
                          :slug,
                          :city,
                          @main_city.slug),
                          :prompt => 'Pilih',
                          class: "form-control kabupaten_kota" %>
                      <% end %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_main_district_id" class="form-control-label">Kecamatan</label>
                      <% if (params[:main_district_id].nil?) || (params[:main_district_id] == "") %>
                        <%= select_tag :main_district_id,
                          options_from_collection_for_select(
                          [],
                          '',
                          ''),
                          :prompt => 'Pilih',
                          class: "form-control kecamatan",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_district_id,
                          options_from_collection_for_select(
                          Main::District.all,
                          :slug,
                          :district,
                          @main_district.slug),
                          :prompt => 'Pilih',
                          class: "form-control kecamatan",
                          "data-toggle": "select" %>
                      <% end %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_main_sub_district_id" class="form-control-label">Kelurahan</label>
                      <% if (params[:main_sub_district_id].nil?) || (params[:main_district_id] == "") %>
                        <%= select_tag :main_sub_district_id,
                          options_from_collection_for_select(
                          [],
                          '',
                          ''),
                          :prompt => "Pilih",
                          class: "form-control kelurahan",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_sub_district_id,
                          options_from_collection_for_select(
                          Main::SubDistrict.all,
                          :slug,
                          :sub_district,
                           @main_sub_district.slug),
                          :prompt => 'Pilih',
                          class: "form-control kelurahan",
                          "data-toggle": "select" %>
                      <% end %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_main_citizen_association_id" class="form-control-label">RW</label>
                      <% if (params[:main_citizen_association_id].nil?) || (params[:main_district_id] == "") %>
                        <%= select_tag :main_citizen_association_id,
                          options_from_collection_for_select(
                          [],
                          '',
                          ''),
                          :prompt => "Pilih",
                          class: "form-control erwe",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_citizen_association_id,
                          options_from_collection_for_select(
                          Main::CitizenAssociation.all,
                          :slug,
                          :citizen_association,
                          @main_citizen_association.slug),
                          :prompt => 'Pilih',
                          class: "form-control erwe",
                          "data-toggle": "select" %>
                      <% end %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_main_neighborhood_association_id" class="form-control-label">RT</label>
                      <% if (params[:main_neighborhood_association_id].nil?) || (params[:main_district_id] == "") %>
                        <%= select_tag :main_neighborhood_association_id,
                          options_from_collection_for_select(
                          [],
                          '',
                          ''),
                          :prompt => "Pilih",
                          class: "form-control erte",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_neighborhood_association_id,
                          options_from_collection_for_select(
                          Main::NeighborhoodAssociation.all,
                          :slug,
                          :neighborhood_association,
                          @main_neighborhood_association.slug),
                          :prompt => "Pilih",
                          class: "form-control erte",
                          "data-toggle": "select" %>
                      <% end %>
                    </div>

                  </div>
                  <div class="col-xl-4">


                    <div class="form-group">
                      <label for="main_patient_main_marital_status_id" class="form-control-label">Status Pernikahan</label>
                      <% if (params[:main_marital_status_id].nil?)  || (params[:main_marital_status_id] == "") %>
                        <%= select_tag :main_marital_status_id,
                          options_from_collection_for_select(
                          Main::MaritalStatus.all,
                          :slug,
                          :marital_status),
                          :prompt => 'Pilih',
                          class: "form-control",
                          "data-toggle": "select" %>
                      <% else %>
                        <%= select_tag :main_marital_status_id,
                          options_from_collection_for_select(
                          Main::MaritalStatus.all,
                          :slug,
                          :marital_status,
                          @main_marital_status.slug),
                          :prompt => 'Pilih',
                          class: "form-control",
                          "data-toggle": "select" %>
                      <% end %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_name_of_parent" class="form-control-label">Nama Orang Tua</label>
                      <%= text_field_tag :name_of_parent, nil, class: 'form-control', value: @name_of_parent %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_phone_number" class="form-control-label">Nomor Telepon</label>
                      <%= text_field_tag :phone_number, nil, class: 'form-control', value: @phone_number %>
                    </div>

                    <div class="form-group">
                      <label for="main_patient_gender" class="form-control-label">Jenis Kelamin</label><br>
                      <% if (params[:gender].nil?) %>

                        <div class="custom-control custom-radio custom-control-inline">
                          <%= radio_button_tag(:gender, "male", false, class: 'custom-control-input') %>
                          <label for="gender_male" class="custom-control-label">Laki-laki</label>
                        </div>
                        <div class="custom-control custom-radio custom-control-inline">
                          <%= radio_button_tag(:gender, "female", false , class: 'custom-control-input') %>
                          <label for="gender_female" class="custom-control-label">Perempuan</label>
                        </div>

                      <% else %>
                        <div class="custom-control custom-radio custom-control-inline">
                          <%= radio_button_tag(:gender, "male", {checked: @gender == 'Male' ? true : false, class: 'custom-control-input'}) %>
                          <label for="gender_male" class="custom-control-label">Laki-laki</label>
                        </div>
                        <div class="custom-control custom-radio custom-control-inline">
                          <%= radio_button_tag(:gender, "female", {checked: @gender == 'Female' ? true : false, class: 'custom-control-input'}) %>
                          <label for="gender_female" class="custom-control-label">Perempuan</label>
                        </div>
                      <% end %>
                    </div>
                  </div>


                </div>
            </div>
            <div class="modal-footer">
              <%= submit_tag 'Search', class: 'btn btn-primary' %>
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
          </div>
        </div>
      </div>
  <% end %>
<% end %>
<% if notice %>
  <div class="notifikasi" data-notifikasi="<%= notice %>"></div>
<% end %>
<%= render partial: 'layouts/body' %>
