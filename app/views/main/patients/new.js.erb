$('#renderNewPatientForm').append(
    '<%= j render(
        partial: 'form',
        locals:{
            main_patient: @main_patient
        })%>'
);

$('#newPatient').on('show.bs.modal', function (e) {
  enableDatepicker();
  enableSelect2();
  apiKelurahan();
});
$('#newPatient').modal('show');